name: Run unit tests

on:
  push:
    branches:
      - '*'

jobs:
  run-tests:
    uses: SimplioOfficial/simplio-app/.github/workflows/_test_application.yml@task/106-remove_generated_files_from_git
    with:
      e2e: false
      unitTests: true
